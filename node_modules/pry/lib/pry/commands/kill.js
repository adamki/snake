(function() {
  var Command, Kill,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Command = require('../command');

  Kill = (function(superClass) {
    extend(Kill, superClass);

    function Kill() {
      return Kill.__super__.constructor.apply(this, arguments);
    }

    Kill.prototype.name = 'kill!';

    Kill.prototype.aliases = ['kill', 'exit!', 'quit!', 'stop!', 'exit.p'];

    Kill.prototype.definition = 'Exits from the entire script.';

    Kill.prototype.execute = function(args, chain) {
      chain.stop();
      process.exit();
      return false;
    };

    return Kill;

  })(Command);

  module.exports = Kill;

}).call(this);
